# 数据库配置
DATABASE_URL=postgres://postgres:postgres@localhost:5432/{DB_NAME}

# AI 配置 (获取 API Key: https://cloud.siliconflow.cn/)
SILICONFLOW_API_KEY=sk-your-siliconflow-api-key-here

# ==================== 高级配置（可选）====================
# 以下配置有默认值，无需修改即可使用
# 如需自定义，取消注释并修改对应值

# 自定义 API 地址（默认: https://api.siliconflow.cn/v1）
# AI_BASE_URL=https://api.openai.com/v1

# 自定义聊天模型（默认: Qwen/Qwen2.5-7B-Instruct）
# 推荐纯文本模型，不要使用 VL（视觉语言）模型
# 可选模型:
# - Qwen/Qwen2.5-7B-Instruct (默认，免费)
# - Pro/Qwen/Qwen2.5-72B-Instruct (高性能)
# - Qwen/Qwen2.5-Coder-7B-Instruct (代码专用)
# CHAT_MODEL=Qwen/Qwen2.5-7B-Instruct

# 自定义嵌入模型（默认: BAAI/bge-large-zh-v1.5）
# EMBEDDING_MODEL=BAAI/bge-base-zh-v1.5

# 自定义向量维度（默认: 1024）
# 注意：修改维度需要重新生成数据库迁移
# EMBEDDING_DIMENSIONS=768
